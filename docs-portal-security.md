# بوابة العملاء — الإعداد

## الرابط الموحّد
`https://موقعك/portal` — نفس الرابط لكل العملاء.

## متغيرات البيئة (Vercel → Settings → Environment Variables)

### البريد الإلكتروني (إلزامي) — Resend
1. سجّل في https://resend.com (مجاني حتى 3,000 رسالة/شهر)
2. أضف نطاقك وتحقق منه (أو استخدم onboarding@resend.dev للتجربة)
3. أنشئ API Key

```
RESEND_API_KEY=re_xxxxxxxxx
MAIL_FROM="مكتب المحاماة <no-reply@yourdomain.com>"
```

### الرسائل النصية (اختياري)
**Unifonic (السعودية):**
```
SMS_PROVIDER=unifonic
UNIFONIC_APP_SID=xxxx
UNIFONIC_SENDER_ID=OFFICE
```
**أو Twilio:**
```
SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=ACxxxx
TWILIO_AUTH_TOKEN=xxxx
TWILIO_FROM=+1xxxxxxxxxx
```

### قناة التحقق الافتراضية
```
PORTAL_OTP_CHANNEL=auto   # auto = SMS إن توفر وإلا البريد | email | sms
```

## كيف يعمل (بوابة عامة — مناسبة للحملات التسويقية)
- **الرابط عام**: انشره في حملتك أو موقعك أو السوشال ميديا. أي شخص يفتحه ويسجّل بنفسه.
- **التسجيل الذاتي**: اسم + جوال + بريد → رمز 6 أرقام على البريد (يثبت ملكيته) → كلمة مرور → الحساب جاهز فوراً.
- **الربط التلقائي**: لو البريد مسجّل مسبقاً لدى المكتب (عميل قديم)، يُفعّل حسابه الموجود ويُربط بقضاياه تلقائياً.
- **الدخول**: بريد + كلمة مرور → رمز تحقق → جلسة 30 يوم.
- **استعادة كلمة المرور**: رمز على البريد → كلمة مرور جديدة.

## الأمان
- رمز التحقق على البريد يمنع انتحال بريد الغير (لا يمكن تفعيل حساب على بريد لا تملكه).
- قفل 15 دقيقة بعد 5 محاولات كلمة مرور، 3 طلبات رمز كل 15 دقيقة، 5 محاولات لكل رمز.
- سجل وصول كامل بملف كل عميل، وتعطيل فوري لأي حساب من لوحة المكتب.
- الروابط الخاصة القديمة `/portal/[token]` لا تعمل بدون جلسة دخول صالحة.
- كل عميل يرى بياناته فقط (فحص ملكية على مستوى كل استعلام + جلسة موقّعة).
